package com.aesopwow.subsubclipclop.domain.request_list.repository;

import com.aesopwow.subsubclipclop.entity.RequestList;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RequireListRepository extends JpaRepository<RequestList, Long> {
}
